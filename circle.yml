dependencies:
  pre:
    - script/ci/prepare.sh
  post:
    - script/ci/docs.sh
  cache_directories:
    - ~/dependencies
    - ~/.mix
    - _build
    - deps
test:
  override:
    - script/ci/test.sh
